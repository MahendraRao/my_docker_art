# My Docker Art Project

This project contains a Dockerized application that displays ASCII art for Lohri festival.

## Usage

To build and run the Docker container:

```bash
docker build -t my_lohri_art .
docker run my_lohri_art

